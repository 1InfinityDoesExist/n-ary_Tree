package com.tree.nary.leetCode;

import com.tree.nary.headends.Node;
import com.tree.nary.headends.Tree;

public class Max_Depth_N_Ary_Tree {
    public static void main(String[] args) {
        Node<Integer> root = new Node<Integer>(0);
        Tree<Integer> n_aryTree = new Tree(root);
        buildNarrayTree(n_aryTree);

    }

    private static void buildNarrayTree(Tree<Integer> tree) {
        

    }
}
